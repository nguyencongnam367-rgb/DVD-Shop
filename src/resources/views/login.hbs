<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Đăng nhập | DVD Shop</title>
    <link rel="stylesheet" href="/css/app.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
    />
  </head>
  <body>
    <header class="site-header">
      <a class="brand" href="/" aria-label="Về trang chủ DVD Shop">
        <img src="/image/logo.png" alt="DVD Shop" />
      </a>
      <a class="cart-link" href="/cart" aria-label="Mở giỏ hàng">
        <i class="fa-solid fa-cart-shopping" aria-hidden="true"></i>
        <span>Giỏ hàng</span>
      </a>
    </header>

    <main class="auth-page">
      <div class="auth-container">

        <h2>Đăng nhập</h2>
        <p class="auth-subtitle">Chào mừng bạn quay lại với DVD Shop</p>

        <form action="/login" method="POST">

          <div class="form-group">
            <label for="email">Email</label>
            <input
              type="email"
              id="email"
              name="email"
              placeholder="Nhập email"
              required
            />
          </div>

          <div class="form-group">
            <label for="password">Mật khẩu</label>
            <input
              type="password"
              id="password"
              name="password"
              placeholder="Nhập mật khẩu"
              required
            />
          </div>

          <button type="submit">Đăng nhập</button>

        </form>

        <p>
          Chưa có tài khoản?
          <a href="/register">Đăng ký</a>
        </p>

      </div>
    </main>
  </body>
</html>